name: ${COMPOSE_PROJECT_NAME:-voxel-destruction-dev}

services:
  web:
    build:
      context: .
    container_name: ${WEB_CONTAINER_NAME:-voxel-destruction-dev-web}
    restart: unless-stopped
    networks:
      - edge

  test:
    image: node:22-alpine
    profiles:
      - tools
    network_mode: none
    working_dir: /app
    volumes:
      - ./:/app:ro
    command:
      - sh
      - /app/scripts/validate.sh

networks:
  edge:
    external: true
